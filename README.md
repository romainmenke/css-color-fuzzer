# CSS Color Fuzzer

- `npm install`
- `node ./test.mjs`

- generates 10.000 random colors
- checks the computed value in webkit, chromium and firefox

----

See [the issues](https://github.com/romainmenke/css-color-fuzzer/issues) for interop problems
